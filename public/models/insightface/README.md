# InsightFace models (buffalo_sc)

Place here:

- `det_500m.onnx` — SCRFD detector
- `w600k_mbf.onnx` — ArcFace MobileFaceNet (512-d)

Download:

```bash
npm run download:insightface
```

Source: Hugging Face `WeChat/buffalo_sc` (InsightFace buffalo_sc pack).
Model id stored in DB: `ArcFace-Buffalo-SC`.
