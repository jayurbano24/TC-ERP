/**
 * Traceability — historial y contexto de equipos (legacy bridge / strangler fig).
 *
 * ARCH-01 seam para `@/lib/database/traceability` usado por `/consulta`.
 */
export * from '@/lib/database/traceability';
