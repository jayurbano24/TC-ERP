/**
 * Platform events — lectura de eventos de dominio para UI (legacy bridge).
 *
 * ARCH-01 seam para `@/lib/database/domainEvents` usado por los banners y
 * timelines de eventos de dominio en `src/components/domain-events`.
 */
export * from '@/lib/database/domainEvents';
